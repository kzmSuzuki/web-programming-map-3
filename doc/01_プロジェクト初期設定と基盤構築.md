# Ticket 01: プロジェクト初期設定と基盤構築

## 目的
React + TypeScript + Vite + Firebase を使った開発基盤を作る。

## スコープ
- Vite（React + TypeScript）初期セットアップ
- 依存導入（`@xyflow/react`、`dagre`、`firebase`、`react-router-dom`）
- ディレクトリ構成の作成（要件記載の構成）
- Firebase初期化コードの雛形作成

## ToDo
- [x] Vite + React + TypeScript プロジェクト作成
- [x] 必須ライブラリを追加
- [x] `src/components/common` などの基本ディレクトリを作成
- [x] Firebase設定ファイル（環境変数参照）を作成
- [x] 共通型・定数の置き場を用意

## 完了条件
- `npm run dev` でアプリが起動する
- 主要ディレクトリとFirebase初期化ファイルが存在する

## 依存
- なし
