# Ticket 09: 管理者による学生進捗管理

## 目的
管理者が学生ごとのノード状態を確認・手動変更できるようにする。

## スコープ
- 学生一覧表示
- 学生ごとのノード進捗一覧
- 状態手動変更（`initial`/`active`/`cleared`）

## ToDo
- [x] 学生一覧を取得して表示
- [x] 学生選択時にノード進捗一覧を表示
- [x] 状態変更UI（セレクト等）を実装
- [x] 変更をFirestoreへ保存
- [x] 変更後の即時反映とエラーハンドリングを実装

## 完了条件
- 任意の学生・任意ノードの状態を管理者が更新できる
- 状態変更が画面に即時反映される

## 依存
- Ticket 07
- Ticket 08
